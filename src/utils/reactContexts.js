import { createContext } from "react";

//export const CalendarContext = createContext('initial value');
const CalendarContext = createContext('initial value');
const AppContext = createContext('initial value');
const AlertContext = createContext('initial value');
export {
  CalendarContext,
  AppContext,
  AlertContext
}